# never commit credentials or API keys
betfair_api_config.json
betfair_api_config*.json
credentials*.json
*.env
.env*
*secret*
*.key
*.pem
*.p12
license.key

# generated / bulky
__pycache__/
reports/*.xlsx
price_log/*.csv
data/
archive/2026-09-15_cleanup/data_generated/

# reports/ outputs are regenerated by the backtests and run to hundreds of MB
# (CSV result frames, pickle caches, HTML equity curves). reports/*.txt and the
# .md write-ups in there are the audit evidence and stay tracked.
reports/*.csv
reports/*.pkl
reports/*.html

# the cloud app is its own repository - don't double-track it here
cloud_app/

# caches and tooling state
.mypy_cache/
.ruff_cache/
.pytest_cache/
.continue/

# spreadsheets, logs, and anything the DB engine drops beside the data
*.xlsx
*.log
*.mdf
*.ldf

# raw RaceIQ page captures (kept for local re-parsing, ~8 KB each zipped)
raceiq_raw/
