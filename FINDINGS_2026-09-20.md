# Findings — 2026-09-20 — what is measured, what is broken, what is not

**Read this before re-running anything.** Every number here was produced today (or is
dated explicitly). If a question is answered below, the answer plus its caveat is the
answer — do not re-derive it from scratch.

---

## 1. Systems and who owns what

| system | code | is it Ben's? |
|---|---|---|
| **Ben (Qas) — five rules** | `cloud_app/our_system.py` (strict picks), `scripts/selection_today.py` (R1–R5 flags) | **yes, verbatim.** `scripts/bens_racecard.py` names his own flags `BEN_flag` / `BEN_flag_soft` |
| Ben (Qas) — daily scan angles | `cloud_app/tips_today.json`, built by `scripts/build_all_selections_cache.py` | **no.** Variants of his rules with extra filters |
| Speed & Stride | `cloud_app/speed_stride_rule.py` | no |
| AI System / AI Top-2 (early) | `scripts/backtest_power_rank*.py`, `scripts/log_power_top2.py` | no |
| Exchange EW Edge | `cloud_app/betfair_ew_service.py` | no — a tool, not a punting system |

**The five rules** (strict = all five, soft = R1+R3+R4):
1. mark falling (below last time out) 2. below last winning mark 3. below career-best
mark 4. proven at today's trip (within 110 yards) 5. top-four last time out.
Scope: **handicaps only**, minimum 5 runners, live runners with a rating and history.

**The three daily-scan angles are NOT the same tests** (`backtest_tips_history.py`):
* Big Weight Drop = mark delta ≤ **−8lb** AND best Topspeed ≥ 60
* Value Qualifier = below last winning mark + ≥1 trip placing + LTO top-4 (**no career-max test**)
* Placed at Trip = **≥3** trip placings + LTO 2nd/3rd + TS ≥ 60

The results ledger stores all of this under the key `"Tips"`; the app displays it as
"Ben (Qas)" via `SYSTEM_DISPLAY`. The ledger's `sub_system` splits angles by drop size
(14 values, mostly one bet) — the app collapses them back to the three.

---

## 2. Historical ROI (Racing Post frame, 2021-01-01 → 2026-09-17)

From `scripts/backtest_tips_history.py`, each-way 1u + 1u place, place terms from field size:

| picks | bets | win % | place % | @RP SP | @Betfair BSP |
|---|---|---|---|---|---|
| every runner (control) | 645,481 | 11.4 | 30.7 | **−18.5%** | *+8.7% — impossible* |
| Big Weight Drop | 44,156 | 11.6 | 31.5 | −13.8% | +8.2% |
| Value Qualifier | 74,106 | 16.4 | 40.7 | −16.4% | −1.6% |
| Placed at Trip | 7,851 | 16.9 | 41.1 | −16.9% | −3.0% |

Big Weight Drop by year @BSP: 2021 +6.0 · 2022 +10.1 · 2023 +7.2 · 2024 +6.6 · 2025 +11.9 · 2026 +5.7.

### ⚠️ The place leg is broken — levels are not usable, only ranking is

The all-runner control cannot be +8.7%, let alone +43% with an extra place. The model
pays the place leg as a **fixed fraction of the BSP win price**, which overpays every
longshot (a 62/1 shot "pays" a 13.4 place dividend for a place the market prices near 7).
Big Weight Drop is the longest-priced angle (avg 28.5), so it is the most flattered.

**Therefore:** the *ranking* (Big Weight Drop > Value Qualifier ≈ Placed at Trip; at SP
all three lose, less than the field) survives; **every BSP ROI level is inflated** by an
unknown amount until the place leg is settled at a real place price.

### Extra places — cannot be answered yet

Re-settling the same picks with +1 / +2 places:

| picks | std | +1 place | +2 places |
|---|---|---|---|
| every runner (control) | +8.7% | +43.1% | +91.0% |
| Big Weight Drop | +8.2% | +34.6% | +64.9% |
| Value Qualifier | −1.6% | +17.0% | +36.1% |
| Placed at Trip | −3.0% | +14.3% | +31.9% |

Those extra-place gains are an artefact of the same broken place leg (+43% for backing
the whole field). **The direction is real** — one extra place lifts the placed share of
Big Weight Drop picks from 31.5% to 43.4% — but the size must be measured against real
place prices (the ledger already records `bf_place_odds` / `early_place_odds`, and the
cloud snapshots now capture bookmaker EW terms + Betfair place back/lay).

---

## 3. Live record — last 5 days (results_ledger.csv, 15-09 → 20-09)

514 logged picks, **373 settled** (20-09 still running). Each-way, 1u+1u, at the recorded price:

| system | bets | win % | implied % | **gap** | ROI early | ROI SP |
|---|---|---|---|---|---|---|
| AI System | 74 | 31.1 | 16.6 | **+14.5pp** | +75.4% | +71.0% |
| Speed & Stride | 96 | 18.8 | 16.0 | +2.8pp | +41.6% | +30.2% |
| **Ben (Qas)** | 176 | 14.2 | — | — | **+4.4%** | **+16.3%** |
| Exchange EW Edge | 13 | 15.4 | 13.2 | +2.2pp | +22.8% | +1.7% |
| AI Top-2 (early) | 14 | 7.1 | 19.5 | −12.4pp | −100% | −74.4% |

**Yes — in the live ledger every system except AI Top-2 is in profit at both price bases.**
But the sample is one day plus scraps: **305 of the 373 settled picks are 19-09**; 15–18
September contribute 1–11 bets per system per day. Read `gap` (strike vs implied), not ROI.

By angle, Ben (Qas) — this answers "Placed at Trip strike rate and ROI":

| angle | bets | strike | implied | gap | placed % | ROI early | ROI SP |
|---|---|---|---|---|---|---|---|
| Big Weight Drop | 63 | 14.3% | 11.7% | **+11.4pp** | 30.2% | +157%* | +148%* |
| Value Qualifier | 138 | 10.1% | 16.3% | −2.8pp | 31.9% | −26% | −13% |
| Placed at Trip | 40 | 5.0% | 15.9% | **−8.5pp** | 20.0% | −87% | −54% |

\* one 41/1 winner (Turnstile, Ayr) supplies 43% of that profit, and 24 of the 63
weight-drop picks have **no SP recorded and no wins** — treat those ROIs as noise.

---

## 4. Ben's real bets — the forward book (not a backtest)

`cloud_app/our_system_forward_ledger.csv`, 1,759 real bets, 2026-02-01 → **2026-09-13**
(sources `BensBets_Master_Feb_to_Sep_2026.csv`, `BensBets_Morning_Master_2026.csv`):

* **at the price taken: +39.9%** (+1,678.8u on 4,202u staked), max DD 157.9u, **8/8 months positive**
* **at Betfair BSP: +7.0%** (+296.2u), max DD 226.8u, 5/8 months positive
* win 17.4% vs market-implied 16.4% → **selection edge ≈ +1.0pp**

Caveats: settled **win-only net 2%** even on the 1,151 rows labelled Each Way (no place legs
recorded); stakes vary 0.5u–4.5u, so ROI must be against staked money, not 2u/pick.
**Frozen at 13-09** — it only advances when Ben's spreadsheet does.

---

## 5. Known broken — do not quote these

| item | state |
|---|---|
| Historical each-way **place leg** (fraction of win BSP) | **broken** — inflates every BSP level (§2) |
| Non-runners scored as −2u losses | **fixed today** in `system_stats.py` (was burying Big Weight Drop: +0.5% vs +8.2%) |
| Forward ledger "Each Way" rows | settled win-only; label wrong or place legs unrecorded |
| `results_ledger` SP missing on **154 of 514** rows | any SP comparison rests on a subset |
| Speed & Stride audited +9.46% / +6.47% | SData feed **died 2026-04-30** |
| Speed & Stride live (RaceIQ) | SPEED +0.09% · STRIDE −23.16% · AGREE **−47.42%** |
| AGREE **+12.42%** still displayed in the app | **stale** — superseded frame, never re-run |
| Power Rank −0.5% / Big Weight Drop +0.5% (old run) | distorted by the non-runner bug; place-leg caveat applies to the new figures too |
| GitHub Actions | **0 runs ever** — the cloud capture has produced nothing yet (§6) |

---

## 6. Infrastructure as it stands

* **Cloud capture** (`cloud_app` repo, GitHub Actions, repo secrets): morning 09:35 UTC, then
  hourly + **T-15/T-10/T-5** + **BSP** on a `*/5` schedule. T-4…T-1 need the laptop
  (`intraday_capture.py` on a 1-minute timer: `pythonw.exe … --log capture.log`).
* **SQL**: the cloud cannot reach LocalDB. `scripts/import_snapshots_to_sql.py --from-git`
  loads `PRODB.dbo.SnapshotOdds` (registry `dbo.SnapshotFiles`), wired as daily pipeline
  **step 4b**. 1,098 rows so far, from the two hand-committed snapshots.
* **Credentials** are not in the repo: User env vars `BETFAIR_APP_KEY/USERNAME/PASSWORD`, with
  fallback `E:\CGMBET\betfair_api_config.json` (both copies must match — they do).
  `python scripts\betfair_creds.py --diagnose`.
* **Blocked on you**: Actions → *Morning odds capture* → **Run workflow**, and *Intraday odds
  capture*. One click each; it also proves the repo secrets exist.
* The site is Streamlit Cloud, redeploying from `main` on push (repo public → Actions free).

---

## 7. Bugs fixed today (so they are not re-found)

| fix | where |
|---|---|
| Exchange EW table could never render (block sat inside the "no data" branch) | `cloud_app/app.py` |
| `.streamlit/secrets.toml` was not gitignored at all | `cloud_app/.gitignore` |
| `%TEMP%` token cache wrote into the repo root on Linux | `betfair_ew_service.py` |
| Betfair venue join dropped every price (`hamilton` vs `Hamilton Park`) | `selection_price_log.py` |
| Results (IRE) name suffixes broke the price↔result join | `selection_price_log.py` |
| `picks --flag any` bailed out; `report` tracebacked with no log | `selection_price_log.py` |
| Betfair place **back** stored, never the **lay** | `morning_capture.py` |
| `system_stats.py` divided the forward book by 2u/pick when stakes vary | `system_stats.py` |
| Non-runners scored as losses | `system_stats.py` |

---

## 8. Next actions, in value order

1. **Settle the place leg at a real place price** (BFSP place / `bf_place_odds` / the snapshot's
   bookmaker place price) and re-run §2 — the one change that makes historical ROI, and the
   extra-place question, real.
2. **Click Run workflow** (both) so the cloud starts feeding prices.
3. **Log Ben's strict five-rule picks as their own ledger system** — his exact rule has no live
   P/L today; it is smeared into three project variants.
4. **Fix the 154 missing SPs / mark voids** so ROI stops resting on subsets.
5. **Re-source or retire the forward book** (frozen 13-09).

